name: Schema Consistency Check
on:
  push:
    branches:
      - main
  pull_request:

concurrency:
  group: schema-consistency-${{ github.head_ref || github.sha }}
  cancel-in-progress: true

jobs:
  check:
    runs-on: ubuntu-latest
    services:
      mariadb:
        image: mariadb:10.11
        env:
          MYSQL_ROOT_PASSWORD: root
          MYSQL_DATABASE: money
          MYSQL_USER: test
          MYSQL_PASSWORD: test
        ports:
          - 3306:3306
        options: --no-healthcheck
    env:
      GRADLE_OPTS: -Dkotlin.compiler.execution.strategy=in-process -Dorg.gradle.workers.max=2 -Dorg.gradle.daemon=false -Dorg.gradle.jvmargs="-Xmx4500m"
    steps:
      - uses: actions/checkout@v6
        with:
          persist-credentials: false
      - uses: actions/setup-java@v5
        with:
          distribution: 'temurin'
          java-version: '25'
      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v6
      - name: mariadb-clientのインストール
        run: sudo apt-get install -y mariadb-client
      - name: MariaDBの起動を待機
        run: |
          for i in $(seq 1 30); do
            mariadb -h 127.0.0.1 -u test -ptest -e "SELECT 1" money 2>/dev/null && echo "MariaDB ready" && break
            echo "Waiting for MariaDB... ($i/30)"
            sleep 3
          done
      - name: SQLファイルを適用
        run: |
          SQL_DIR="backend/datasource/db/src/jvmMain/resources/sql"
          mariadb -h 127.0.0.1 -u test -ptest money < "$SQL_DIR/master.sql"
          for f in $(ls "$SQL_DIR"/*.sql | grep -v "master.sql" | sort); do
            echo "適用中: $f"
            mariadb -h 127.0.0.1 -u test -ptest money < "$f"
          done
      - name: jOOQコードを生成
        env:
          DB_USER: test
          DB_PASS: test
        run: ./gradlew :backend:datasource:db:schema:generateDbCode
      - name: スキーマの差分を確認
        run: git diff --exit-code backend/datasource/db/schema/src/jvmMain/kotlin
