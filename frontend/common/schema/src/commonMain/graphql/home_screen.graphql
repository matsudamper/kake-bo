query RootHomeTabScreen {
    user {
        userMailAttributes {
            mailCount
        }
        importedMailAttributes {
            count(query: {
                isLinked: false
            })
        }
    }
}
