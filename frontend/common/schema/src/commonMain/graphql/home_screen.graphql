query HomeScreen {
    user {
        userMailAttributes {
            mailCount
        }
        importedMailAttributes {
            count(query: {
                isLinked: false
            })
        }
    }
}
