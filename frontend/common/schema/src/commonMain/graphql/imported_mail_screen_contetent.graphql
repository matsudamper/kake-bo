query ImportedMailContentScreen($id: ImportedMailId!) {
    user {
        importedMailAttributes {
            mail(id: $id) {
                id
                html
            }
        }
    }
}
