query LoginScreenFidoInfo {
    fidoLoginInfo {
        domain
        challenge
    }
}

mutation LoginScreenChangeSessionName($name: String!) {
    userMutation {
        changeSessionName(name: $name) {
            isSuccess
        }
    }
}
